# npu-systems-lab

NPU 소프트웨어 스택 전반을 다루는 실습 프로젝트 모음. 모델 최적화, 양자화,
멀티 프레임워크 추론, 런타임 통합, 프로파일링.

번호가 붙은 디렉터리 하나가 프로젝트 하나이고, 각자 README와 코드와 측정
결과를 갖는다.

## 문서 작성 규칙

**사람이 읽는 산문은 전부 한글로 쓴다.** README, 설계 문서, 결과 해설, 회고가
여기 해당한다.

예외는 두 가지다.

- **코드** -- 식별자, 함수명, docstring, 인라인 주석은 영문. 공개 저장소의 코드이고
  영문이 표준이다.
- **원문 인용** -- 에러 메시지, 로그, 명령 출력, 라이브러리 API 이름은 번역하지
  않는다. 번역하면 검색이 안 된다.

git 커밋 메시지는 영문을 유지한다.

## 이 레포가 담는 것과 담지 않는 것

| 담는다 | 담지 않는다 |
|---|---|
| 코드, 설정, 측정 결과, 결과 해설 | 모델 가중치, 가상환경, 변환 산출물 |
| 설계 판단과 그 근거 | 커리어 관련 문서 -- JD 분석, 역량 자기평가, 지원 대상 |
| 여기 있는 코드를 돌리기 위한 실행 계획 | 어떤 프로젝트를 왜 고르는지에 대한 판단, 회고 |

**기준은 "clone 한 사람이 그대로 실행할 수 있는가"다.** 3단계를 재현하려면 무엇을
먼저 검증해야 하는지 같은 실행 계획은 그 사람에게 필요하므로 여기 둔다
(`NN-*/0N-*-plan.md`). 반면 이 프로젝트를 왜 골랐는지, 어떤 역량이 부족한지는
재현에 필요 없고 커리어 정보를 드러내므로 **별도의 비공개 저장소**에 둔다. 이 레포는
그 저장소에 의존하지 않는다.

용량이 큰 산출물은 `.gitignore`에 있다. `results/` 아래의 요약 표와 그래프는
커밋하지만 원시 텐서 덤프는 커밋하지 않는다.

## 두 머신에 걸쳐 작업하기

편집은 로컬(macOS)에서, 실행은 GPU 머신에서 한다. 로컬에는 CUDA가 없고 GPU
머신에는 에디터가 없다.

```bash
./sync.sh check          # 무엇이 다른가? 조용하면 일치
./sync.sh push           # 로컬 -> 서버, 소스만
./sync.sh pull-results   # 서버 -> 로컬, 커밋할 결과물만
```

접속 대상은 환경 변수로 바꾼다: `NPU_HOST=myserver ./sync.sh push`

| 수단 | 역할 |
|---|---|
| `sync.sh push` (rsync) | **전송** -- 커밋 없이 코드를 돌린다 |
| git | **이력** -- 롤백과 공개 산출물 |
| `sync.sh check` | **어긋남 검사** -- 내용 비교 |

전송에 git이 아니라 rsync를 쓰는 이유는 속도가 아니라 **커밋을 강제하지 않기
때문**이다. 반쯤 깨진 실험마다 커밋하면 이력이 `wip`으로 채워진다.

`check`는 시각이 아니라 checksum으로 비교한다. git이 clone과 pull에서 mtime을
새로 찍기 때문에, 시각 기반 비교는 git 조작 직후마다 전 파일을 "바뀌었다"고
보고하고 결국 아무도 읽지 않게 된다.

**서버에서 파일을 편집하지 말 것.** `push`가 `--delete`로 덮어쓴다. 꼭 해야
한다면 `check`로 무엇이 사라질지 먼저 확인할 것.

## 주장하기 전에 검증할 것

이 레포의 목적이 측정이므로, 측정 환경 자체가 거짓말하지 않는지 먼저 본다.

- **import 성공은 GPU 사용의 증거가 아니다.** 조용히 CPU로 도는 환경이 흔하다.
  실제 장치 접근을 확인할 것 -- PyTorch는 진짜 matmul을 한 번 돌리고, 컨테이너
  안에서는 `nvidia-smi`, llama.cpp는 `llama-cli --list-devices`.
- **부재는 전수 탐색일 때만 증거다.** 목록 API를 잘린 페이지로 읽고 "없다"고
  단정하지 말 것. 끝까지 넘기거나, "상위 N건에서는 보이지 않음"이라고 쓸 것.
- **같은 이름이 같은 물건은 아니다.** 체크포인트를 비교할 때는 가중치 해시뿐
  아니라 설정 파일까지 대조한다. 토크나이저 설정이 다르면 같은 가중치라도 다른
  숫자가 나온다.

## 프로젝트별 규약

각 프로젝트 디렉터리는 다음을 갖는다.

- `README.md` -- 답하려는 질문, 방법, 설계 판단과 근거, 결과 표
- `tests/` -- 모델 없이 돌아가는 자체 검사. 뼈대의 비자명한 로직을 지킨다
- `results/` -- 측정 결과. 커밋한다

자체 검사는 모델 의존이 없으므로 어디서든 돌아간다. 코드를 건드렸으면 먼저 돌린다.

```bash
cd <프로젝트 디렉터리> && python -m tests.test_harness
```

비자명한 로직(분기, 파서, 수치 계산)을 추가하면 **실패할 수 있는 검사 하나**를
같이 남긴다. 프레임워크나 픽스처는 쓰지 않는다. 자명한 한 줄에는 검사를 붙이지
않는다 -- 검사에도 YAGNI가 적용된다.

측정값이 최초 가정과 어긋나면 **가정과 정정을 둘 다 남긴다.** 대개 정정 쪽이
더 흥미롭다.
